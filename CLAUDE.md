# このリポジトリの編集ルール

積読・進捗トラッカー。**HTML 1ファイルで完結する Web アプリ**で、ビルド工程も外部ライブラリも
サーバーも無い。`git push` したものがそのまま `https://kennyfujita.github.io/tsundoku-tracker/`
で動く。

もとは個人サイト `KennyFujita.github.io` の `tools/tsundoku/` にあった。2026年8月に
独立したリポジトリへ切り出した（プロフィールに作品として並べるため）。**サイト側にはもう無い。**
サイトの `works.html` / `ja/works.html` の制作物カードからここへリンクしている。

## 構成

```
index.html    アプリ本体。HTML・CSS・JavaScript を全部内包している
README.md     使い方・データの扱い・取得先ごとの当たり外れ
LICENSE       MIT
.nojekyll     GitHub Pages の Jekyll 処理を止める
```

## 守ること

### 1ファイルに内包する

`<style>` も `<script>` も外部ファイルに切り出さない。理由は、**保存して手元で開いてもそのまま
動くようにするため**。README に「右クリック →『別名で保存』で自分の道具にできる」と書いてあり、
外部ファイルを参照する作りにすると、ダウンロードした人の手元で色やロジックが壊れる。

同じ理由で **Web フォントを読み込まない**（OS 標準書体のみ）。

### 色は必ず CSS 変数

`:root` に定義し、直書きしない。**ライトとダークの両方を書く** —
`prefers-color-scheme` のメディアクエリと `:root[data-theme="light"|"dark"]` の
両方が要る（トークンのブロックは全部で4つ）。片方だけ足すとテーマ切り替えで破綻する。

### ビルド工程・JS フレームワーク・外部ライブラリを足さない

素の DOM 操作だけで書く。

### 利用者のデータをコミットしない

データは `localStorage`（キー `tsundoku.v1`）にのみ置く。テスト用の JSON もここには入れない。

---

## 外部 API への通信

ISBN を調べたときだけ、**3つを順に引いて欠けている項目を埋める**。得意分野が違うので3つとも要る。
通信先を増やすときはここに追記する。

| 通信先 | 役割 | 実測 |
|---|---|---|
| `api.openbd.jp` | **新刊**。JPRO に発売前から載るのでここでしか拾えない本がある | 専門書のカバー率 6/10、ページ数 1/12 |
| `ndlsearch.ndl.go.jp` (SRU) | **著者とページ数と分類**。納本制度で収録が厚い。新刊は目録作成のラグで載らない | 届いた3件は3件とも著者・ページ数が揃った |
| `www.googleapis.com/books` | **洋書**の受け皿 | キー無しの共有クォータですぐ 429。当てにしない |

### 国立国会図書館サーチの叩き方（重要）

- エンドポイントは **SRU の方**（`/api/sru`）。`/api/opensearch` は **CORS ヘッダを返さない**ので
  ブラウザからは呼べない。以前これを混同して「NDL は使えない」と誤って結論した経緯がある
- **URL に毎回ユニークなパラメータ（`&_=` + タイムスタンプ）を付ける。これは必須。**
  NDL の前段の CloudFront は Origin をキャッシュキーに含めないため、キャッシュに当たると
  `access-control-allow-origin` の付かない応答が返り、ブラウザが遮断する。実測では
  `x-cache: Miss` なら ACAO あり、`Hit` なら ACAO なしと完全に対応していた。
  **キャッシュ避けを消すと、たまに動いてたまに失敗するという厄介な壊れ方をする**
- `recordSchema=dcndl`。`dcndl_simple` はエラーになる
- クエリは `query=isbn=9784873115658`（引用符は不要）
- 応答は `recordData` の中身がエスケープされているので、**DOMParser で2段階に解く**
- 著者は `dc:creator`（`dcterms:creator` は RDF の塊なので使わない）
- ページ数は `dcterms:extent`（例 `237p ; 21cm`）から `(\d+)\s*p` で取る
- **同時アクセス制限がある。** 連続で叩くと `Too Many Requests` が返る。
  1回だけ2秒待って再試行する実装にしてある。まとめて引く処理を足さないこと

### 分類（NDC）も NDL からしか取れない

`dcterms:subject` の `rdf:resource` に `http://id.ndl.go.jp/class/ndc9/548.31` の形で入っている。
同じ要素に `class/ndlc/…`（NDL 独自分類）も並ぶので、**`class/ndc\d*/` で絞ること**。
第1桁が大分類（0総記 1哲学 2歴史 3社会科学 4自然科学 5技術 6産業 7芸術 8言語 9文学）。
openBD の Cコード（`onix.DescriptiveDetail.Subject`）は試した範囲で常に `null` だった。

### 著者名の整形

**openBD の著者は図書館の典拠形**（`三浦,宏文,1938-2020`）で人名として読めない。
`tidyAuthor()` で整形しているが、NDL が返す `dc:creator`（`三浦宏文 著`）の方が読みやすいので
取れた場合は上書きしている。

### API キーを要求するサービスは足さない

公開ページにキーが載る上、リファラ制限をかけるとダウンロードして `file://` で開いた配布版が
動かなくなる（持ち帰って使ってもらう方針と衝突する）。

---

## グラフの色（`--cat-1` 〜 `--cat-5` ＋ `--cat-other`）

**配色を目分量で決めないこと。検証スクリプトを通すこと。**

**背表紙の装丁色（`CLOTH`）をグラフに流用してはいけない。** 積み上がった本の質感のために
暗く彩度を落としてあるので、隣り合う扇を見分けられない。検証にかけた結果:

```
装丁色6色  → FAIL  最悪ペア #5C3A50↔#4A4E56  ΔE 2.5（2型色覚）/ 6.3（通常の色覚）
                  ※通常の色覚でも見分けられない。彩度も基準割れでグレーに見える
採用した5色 → PASS  明暗どちらも。最悪隣接 ΔE 9.1（light）/ 8.4（dark）
```

ライトモードは4色が背景とのコントラスト 3:1 未満のため **relief 条件**が付く。
これは**凡例（名前＋冊数＋割合）と「表で見る」を必ず出すこと**で満たしている。
どちらかを消すと条件を割るので消さないこと。

区分は **6つまで**（上位5分類＋「その他」）。6色目を作らず残りをまとめている。
「その他」は識別色ではなく残余なのでグレー（`#898781`、両モードで 3:1 以上）。

---

## カメラでのバーコード読み取り

`BarcodeDetector` API を使う。**Chrome（Android / デスクトップ）のみ。** Safari は未対応。

起動時に `initScan()` が次を全部確認してからボタンを出す。ひとつでも欠けたらボタンを出さず、
代わりに「この環境では使えません」の一言を出す（**出さないと不具合と区別がつかない**）。

- `BarcodeDetector` / `navigator.mediaDevices.getUserMedia` / `window.isSecureContext`
- `await BarcodeDetector.getSupportedFormats()` に `"ean_13"` が含まれること

`startScan()` は `getUserMedia` 以降を丸ごと `try/catch` で包み、**失敗したら必ず `stopScan()` を
呼んでカメラを止める**。`await video.play()` と `new BarcodeDetector(...)` は
どちらも投げうるので、保護を外さないこと（外すとカメラが点いたまま無反応になる）。

読み取りループは `/^97[89]/` で絞る。日本の本には価格用の `192…` バーコードが並んでいるため。

カメラは HTTPS か `localhost` でしか使えない。`file://` で開くとボタンが出ないのは**正しい挙動**。

---

## 公開しているものとしての体裁

制作物として公開しているので、次を保つこと。

1. **`noindex` を付けない**（SEO 一式 `description` / Open Graph / canonical は入っている）
2. **サンプル投入ボタン**を残す。初見の人は空の画面では何も分からない。
   `sample: true` を付けて登録し、「サンプルを消す」でまとめて消せる。
   **1冊も無いときだけ**出る（既に本がある人には見えない）。**これは忘れやすい**
3. URL を変えたら `index.html` の `canonical` と `og:url`、`README.md` の冒頭、
   そしてサイト側 `works.html` / `ja/works.html` のリンクを揃えて直す

---

## 端末間の同期について（未実装・将来やるかもしれない）

データは `localStorage` にあり、**ブラウザごと・端末ごとに隔離されている**。スマホと PC は
同じ URL を開いても別々の箱を見る。同期は入れていない。移すには JSON の書き出し/読み込みを使う。

なお `localStorage` の単位は**ドメイン**なので、`kennyfujita.github.io` の中でパスが変わっても
データは引き継がれる（`tools/tsundoku/` → `tsundoku-tracker/` の移設でも消えなかった）。

本人の希望は **Google アカウントでの同期**。実装するなら Google Drive API + OAuth 2.0（PKCE）で、
スコープは `drive.appdata`（Drive の隠しフォルダにアプリ専用で保存する。ユーザーの他のファイルに
触らないので権限として最小）。

判断材料として残しておくこと:

- **OAuth のクライアント ID は秘密情報ではない。** SPA は公開クライアントとして設計されているので、
  HTML に載ること自体は問題にならない（一度これを代償として挙げたが誤り）
- **本当の制約は「承認済み JavaScript 生成元」にドメイン登録が要ること。**
  `https://kennyfujita.github.io` は登録できるが、**ダウンロードして `file://` で開いた配布版は
  認証が通らない**。持ち帰って使ってもらう方針と衝突するので、配布版は同期なしで動く作りにする必要がある
- **双方向同期は競合解決という別の問題を呼ぶ。** スマホで 120p、PC で 80p と記録したときどちらを
  採るか決めないといけない。`log` は日付付きなので、**日付ごとに大きい方を採ってマージする**のが
  素直（進捗は単調増加のため）。単純な上書きにはしないこと
- 実装するまでは、README の「控えを取る・持ち運ぶ」の説明を変えないこと

---

## 確認とデプロイ

```sh
# ローカル確認（カメラを試すなら localhost で開くこと。file:// では出ない）
python3 -m http.server 8765

# 公開
git add -A && git commit -m "..." && git push
```

`git push` から反映まで1分ほど。
